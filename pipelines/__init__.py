"""Domain pipelines — twelve end-to-end processing pipelines for RKGB.

Each sub-package represents one domain pipeline. Pipelines contain
orchestration logic only; all business logic lives in the application
layer and is accessed via command and query dispatch.
"""
