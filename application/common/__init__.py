"""Common base types shared across all application capability slices.

This package will contain abstract base classes for commands, queries,
results, and the command/query bus interfaces in a future step.
"""
